name: CI

on: [push]

jobs:
  build:

    runs-on: self-hosted
    
    steps:
    - uses: actions/checkout@v1
    - name: Crear la imagen de docker
      run: docker build -t lab-final:1.0 .
    - name: Imagen creada
      run: echo imagen creada
    - name: npm install
      run: npm install
    - name: npm test
      run: npm test  
    - name: Deploy al cluster k3d
      run: kubectl apply -f .\deployment.yaml