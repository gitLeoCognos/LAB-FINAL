apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: in-lab-info
  annotations:
    ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ci-lab-info
            port:
              number: 80